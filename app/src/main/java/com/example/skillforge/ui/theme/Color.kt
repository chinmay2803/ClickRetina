package com.example.skillforge.ui.theme

import androidx.compose.ui.graphics.Color

// TODO: eyedrop these from the reference screenshots to match exactly.
// These are reasonable starting points for a "cream + teal" light theme.

val Cream = Color(0xFFFBFAF8)        // app background
val CreamCard = Color(0xFFFFFFFF)     // card surfaces on top of cream
val Teal = Color(0xFF0FB5A4)          // primary accent (buttons, active states)
val TealDark = Color(0xFF0A1413)      // video player/dark background
val TealSoft = Color(0x1A0FB5A4)      // light teal (10% opacity) for badges

val TextPrimary = Color(0xFF1A1A1A)
val TextSecondary = Color(0xFF9A9890)
val TextLightMuted = Color(0xFF8A8880)
val TextBody = Color(0xFF46463F)
val Divider = Color(0xFFECEBE6)       // border color #ECEBE6

val BeginnerBadge = Color(0x1A0FB5A4)  // rgba(15, 181, 164, 0.1)
val BeginnerBadgeText = Color(0xFF0FB5A4)

val IntermediateBadge = Color(0x2EFCBF24)  // rgba(251, 191, 36, 0.18)
val IntermediateBadgeText = Color(0xFFE0992B)

